const Colour = artifacts.reqiure('./Colour.sol');

require('chai')
  .use(require('chai-as-promised'))
  .should()

contract('Colour', (accounts) => {

  describe('deployment,', async() => {
    it('deploys successfully', async() => {
      
    })
  })
})